{
  "name": "cloud-explorer",
  "version": "2.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "postinstall": "npm run build",
    "start": "http-server dist -p ${PORT:-8080}",
    "test": "jest",
    "test:watch": "watch 'npm test' src/ __tests__/",
    "build": "browserify src/js/App.js -o dist/cloud-explorer.js -t [ babelify ]",
    "watch": "live-reload dist/ --port 35729 & watch 'npm run build' src/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^0.9.0",
    "babel-polyfill": "^6.9.1",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.11.1",
    "babel-preset-stage-0": "^6.5.0",
    "babelify": "^7.3.0",
    "browserify": "^13.0.1",
    "react": "^15.2.0",
    "react-dom": "^15.2.0"
  },
  "devDependencies": {
    "babel-jest": "^13.2.2",
    "jest-cli": "^13.2.3",
    "live-reload": "^1.1.0",
    "react-addons-test-utils": "^15.2.0",
    "watch": "^0.19.1"
  }
}
